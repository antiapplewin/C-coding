/*#include <stdio.h>
#include <string.h>

struct Person {
  char name[20];
  int age;
  double height;
};

int main() {
  struct Person m1;
  struct Person m2 = {"허난설헌", 13, 159.9};

  strcpy(m1.name, "허균");
  m1.age = 19;
  m1.height = 168.34;

  printf("m1's name, age, height : %s %d %lf\n", m1.name, m1.age, m1.height);
  printf("m2's name, age, height : %s %d %lf\n", m2.name, m2.age, m2.height);

  struct Person m3;
  scanf("%s %d %lf", m3.name, &m3.age, &m3.height);
  printf("m3's name, age, height : %s %d %lf\n", m3.name, m3.age, m3.height);

  struct Person m4;
  scanf("%s %d %lf", m4.name, &m4.age, &m4.height);
  printf("your name, age, height : %s %d %lf\n", m4.name, m4.age, m4.height);
}#*/